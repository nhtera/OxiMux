# file-size ratchet allowlist
#
# Files grandfathered over the 3000-LOC hard cap, recorded at their current
# non-blank LOC. An allowlisted file may shrink freely but fails the lint the
# moment it grows past the budget here — so this list can only ratchet down.
# Drop a row once its file falls under the cap (the lint prints STALE to nudge).
#
# Format: <repo-relative-path> <loc-budget>

# The agent-chat view and its composer. Both predate the cap and are the two
# largest files in the tree; splitting them is a real refactor of the live chat
# surface, not a lint fix, so they are frozen here at their current size instead.
# Neither may grow by a single line while these rows stand.
#
# mod.rs was once raised (8187 -> 8251) to absorb the remote session-registry
# seam, on the condition that the number come back down. It has: the transcript
# rendering and its scroll primitives now live in `agent_chat/transcript.rs`,
# and the budget below is the post-extraction size. That was the only exception
# ever made to "ratchet down only", and it is now closed.
apps/desktop/src/shell/agent_chat/mod.rs 5315
apps/desktop/src/shell/agent_chat/composer.rs 3848
